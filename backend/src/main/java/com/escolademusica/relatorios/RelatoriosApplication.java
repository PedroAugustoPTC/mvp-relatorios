package com.escolademusica.relatorios;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Ponto de entrada da aplicacao Spring Boot do backend de relatorios da escola de musica. */
@SpringBootApplication
public class RelatoriosApplication {

  public static void main(String[] args) {
    SpringApplication.run(RelatoriosApplication.class, args);
  }
}
